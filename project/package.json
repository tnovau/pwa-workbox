{
  "name": "workbox-lab",
  "version": "1.0.0",
  "description": "a lab for learning workbox",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy": "copyfiles -u 1 src/**/**/* src/**/* src/* build",
    "injectManifest": "workbox injectManifest workbox-config.js",
    "build": "npm run copy && npm run injectManifest",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "4.17.1"
  },
  "devDependencies": {
    "@types/workbox-sw": "4.3.0",
    "copyfiles": "2.2.0",
    "workbox-cli": "5.1.2"
  }
}
